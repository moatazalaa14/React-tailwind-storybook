import React from "react";
import {ImagePoll} from "./imagepoll";

export default {
  title: "organisms|ImagePoll",
  component: ImagePoll,
  
};

const Template = (args) => <ImagePoll {...args} />;

export const Primary = Template.bind({});
Primary.args = {
  
  
};
